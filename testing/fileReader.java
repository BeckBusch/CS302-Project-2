package testing;

import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;
import java.util.ArrayList;

class fileReader {
    public static void main(String[] args) {


        /*
    }
        BufferedReader objReader = null;
        try {
            boolean start = true;
            String strCurrentLine;
            objReader = new BufferedReader(new FileReader("testing\\database.csv"));
            while ((strCurrentLine = objReader.readLine()) != null) { //while we are still reading lines

                if (start) { //if it's the first line save the values as the headings
                    String[] headings = strCurrentLine.split(",", 0);
                    start = false;
                    gaming test
                }

            }

        } catch (IOException e) {
            e.printStackTrace();
        } finally {
            try {
                if (objReader != null)
                    objReader.close();
            } catch (IOException ex) {
                ex.printStackTrace();
            }
        }
        */
    }
}
